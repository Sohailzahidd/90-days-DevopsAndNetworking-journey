# Day 2 - Linux File System

## Key Directories:

- / → Root directory
- /home → User personal files
- /etc → Configuration files
- /var → Logs and system data
- /bin → Basic system commands

## Learning:
Linux organizes everything in a hierarchical structure starting from root (/).
